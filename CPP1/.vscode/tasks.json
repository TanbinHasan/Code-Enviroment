{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build and run",
      "type": "shell",
      "command": "g++ -std=c++20 \"${file}\" -o \"${fileBasenameNoExtension}\" && ./${fileBasenameNoExtension} < _input.txt > _output.txt",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
